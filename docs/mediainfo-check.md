# Aggregate MediaInfo

Save the MediaInfo in Text format of multiple files (e.g. of a directory of files) into a file `mediainfo.txt` and run this script to get aggregated MediaInfo of all the files:

```shell
node mediainfo-check.js ./mediainfo.txt
```
