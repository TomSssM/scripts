# Check Video Files

Check that video files inside the current directory `.` are valid by using `eac3to` utility:

```shell
bash eac3to-check.sh
```

---

Note that all the files inside the current directory `.` are supposed to be video files (e.g. MKV Files)
